Use AppNavigation as the adaptive top-level navigation surface; the production generator supplies Lucide icons.

```jsx
<AppNavigation items={pages} activeId="overview" onNavigate={setPage} />
```

Keep mobile primary destinations to four plus a More destination.
