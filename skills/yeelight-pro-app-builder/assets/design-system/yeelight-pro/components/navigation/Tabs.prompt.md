Use Tabs to switch peer views without changing the route hierarchy.

```jsx
<Tabs items={views} value={view} onChange={setView} />
```

Keep labels short, preserve a controlled value, and do not place destructive actions in tabs.
