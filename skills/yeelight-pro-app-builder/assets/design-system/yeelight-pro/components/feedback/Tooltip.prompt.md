Use Tooltip for short supplementary labels, never for required instructions or errors.

```jsx
<Tooltip content="设备响应时间"><button>延迟</button></Tooltip>
```

The trigger must remain keyboard focusable and the content should fit in one or two short lines.
