Use Button for explicit commands and IconButton only when a familiar icon has an accessible label.

```jsx
<Button variant="primary">执行情景</Button>
<IconButton label="重新同步">...</IconButton>
```

Variants: `primary`, `secondary`, `ghost`; use `busy` during asynchronous work.
