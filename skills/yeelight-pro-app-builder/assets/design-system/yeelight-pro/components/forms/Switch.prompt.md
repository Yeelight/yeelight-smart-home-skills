Use Switch for immediate binary settings such as automation enablement or device power.

```jsx
<Switch label="离家自动化" checked={enabled} onChange={setEnabled} />
```

Render explicit state text in addition to the track position; use Checkbox instead for batch selection.
